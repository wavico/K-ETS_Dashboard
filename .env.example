# K-ETS Dashboard 환경 변수 설정 예시
# 이 파일을 .env로 복사하여 사용하세요

# ===================================
# vLLM 설정 (로컬 언어모델)
# ===================================
USE_VLLM=true
VLLM_BASE_URL=http://localhost:8000/v1
VLLM_MODEL_NAME=gpt-4-turbo
VLLM_API_KEY=EMPTY

# vLLM 서버 실행 방법:
# Windows:
#   set VLLM_USE_TRITON_FLASH_ATTN=0
#   venv\Scripts\python.exe -m vllm.entrypoints.openai.api_server --model Qwen/Qwen3-30B-A3B --host 0.0.0.0 --port 8000 --served-model-name gpt-4-turbo --trust-remote-code
#
# Linux/Mac:
#   export VLLM_USE_TRITON_FLASH_ATTN=0
#   python -m vllm.entrypoints.openai.api_server --model Qwen/Qwen3-30B-A3B --host 0.0.0.0 --port 8000 --served-model-name gpt-4-turbo --trust-remote-code

# ===================================
# 외부 AI 모델 설정 (백업용)
# ===================================
# vLLM을 사용하지 않을 경우 아래 API 키 설정
OPENAI_API_KEY=
OPENAI_MODEL=gpt-3.5-turbo
UPSTAGE_API_KEY=

# ===================================
# 서버 설정
# ===================================
HOST=0.0.0.0
PORT=8000
DEBUG=false
ENVIRONMENT=development

# ===================================
# 보안 설정
# ===================================
SECRET_KEY=your-secret-key-change-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ===================================
# 데이터베이스 설정
# ===================================
DATABASE_URL=sqlite:///./kets_dashboard.db

# ===================================
# CORS 설정
# ===================================
ALLOWED_HOSTS=["http://localhost:3000", "http://localhost:8000", "http://127.0.0.1:3000"]

# ===================================
# 데이터 폴더 설정
# ===================================
DATA_FOLDER=data
UPLOAD_FOLDER=uploads
MAX_FILE_SIZE=104857600

# ===================================
# 로깅 설정
# ===================================
LOG_LEVEL=INFO
LOG_FILE=logs/kets_dashboard.log

# ===================================
# 외부 API 설정
# ===================================
KOREA_ENERGY_API_KEY=
ENVIRONMENT_API_KEY=

# ===================================
# 벡터 데이터베이스 (선택사항)
# ===================================
PINECONE_API_KEY=

# ===================================
# 캐시 설정 (선택사항)
# ===================================
REDIS_URL=redis://localhost:6379
CACHE_TTL=3600

# ===================================
# Prophet 예측 모델 설정
# ===================================
PROPHET_CHANGEPOINT_PRIOR_SCALE=0.05
PROPHET_YEARLY_SEASONALITY=true
PROPHET_WEEKLY_SEASONALITY=true
